[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["python -m venv .venv", ".venv/bin/pip install -r requirements.txt"]

[start]
cmd = "bash start.sh"
mounts = ["/app/products", "/app/plugins", "/app/themes"]
